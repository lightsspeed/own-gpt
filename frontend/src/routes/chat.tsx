import { ChatLayout } from '@/components/chat/ChatLayout';

/** Legacy chat — untouched. Will be replaced by ChatWorkspace in Phase 2. */
export function LegacyChatPage() {
  return <ChatLayout />;
}
